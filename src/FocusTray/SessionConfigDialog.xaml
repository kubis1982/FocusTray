<Window x:Class="FocusTray.SessionConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Start Focus Session" 
        MinHeight="260" MinWidth="300" 
        Height="230" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Task Description" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="TaskDescriptionTextBox" Grid.Row="1" 
                 Margin="0,0,0,15" 
                 Padding="5"
                 MaxLength="100"/>

        <TextBlock Grid.Row="2" Text="Duration (minutes)" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="DurationTextBox" Grid.Row="3" 
                 Margin="0,0,0,15" 
                 Padding="5"
                 Text="25"
                 MaxLength="4"/>

        <CheckBox x:Name="TeamsSyncCheckBox" Grid.Row="4" 
                  Content="Sync status with Microsoft Teams"
                  Margin="0,0,0,15"
                  IsEnabled="False"
                  ToolTip="Teams integration will be available in next release"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Start" Width="80" Height="30" 
                    Margin="0,0,10,0" 
                    IsDefault="True"
                    Click="StartButton_Click"/>
            <Button Content="Cancel" Width="80" Height="30" 
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
