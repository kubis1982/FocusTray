<Window x:Class="FocusTray.SessionConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FocusTray.Converters"
        Title="Start Focus Session" 
        MinHeight="280" MinWidth="400" 
        Height="Auto" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- JIRA Integration Section -->
        <CheckBox x:Name="UseJiraCheckBox" 
                  Grid.Row="0" 
                  Content="Use JIRA issue" 
                  IsChecked="{Binding UseJiraIssue}"
                  Margin="0,0,0,10"
                  Visibility="{Binding IsJiraEnabled, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <Grid Grid.Row="1" 
              Margin="0,0,0,15"
              Visibility="{Binding UseJiraIssue, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ComboBox x:Name="JiraIssuesComboBox"
                      Grid.Column="0"
                      ItemsSource="{Binding JiraIssues}"
                      SelectedItem="{Binding SelectedIssue}"
                      DisplayMemberPath="DisplayText"
                      IsEnabled="{Binding IsLoadingIssues, Converter={StaticResource InverseBoolConverter}}"
                      Margin="0,0,10,0"/>
            
            <Button Grid.Column="1"
                    Content="↻"
                    Width="30"
                    Height="30"
                    Command="{Binding LoadJiraIssuesCommand}"
                    ToolTip="Refresh JIRA issues"/>
        </Grid>

        <TextBlock Grid.Row="2" 
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Manual Task Description -->
        <TextBlock Grid.Row="3" 
                   Text="Task Description" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"
                   Visibility="{Binding UseJiraIssue, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
        
        <TextBox x:Name="TaskDescriptionTextBox" 
                 Grid.Row="4" 
                 Text="{Binding TaskDescription, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15" 
                 Padding="5"
                 MaxLength="100"
                 Visibility="{Binding UseJiraIssue, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <!-- Duration -->
        <TextBlock Grid.Row="5" 
                   Text="Duration (minutes)" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>
        
        <TextBox x:Name="DurationTextBox" 
                 Grid.Row="6" 
                 Text="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15" 
                 Padding="5"
                 MaxLength="4"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="8" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Start" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0" 
                    IsDefault="True"
                    Click="StartButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30" 
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
