<Window x:Class="FocusTray.SessionConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FocusTray.Converters"
        Title="Start Focus Session" 
        Height="320" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- JIRA Integration Section (visible when logged in) -->
        <Grid Grid.Row="0" 
              Margin="0,0,0,15"
              Visibility="{Binding IsJiraEnabled, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="JiraIssuesComboBox"
                      Grid.Column="0"
                      ItemsSource="{Binding JiraIssues}"
                      SelectedItem="{Binding SelectedIssue}"
                      DisplayMemberPath="DisplayText"
                      IsEnabled="{Binding IsLoadingIssues, Converter={StaticResource InverseBoolConverter}}"
                      Margin="0,0,10,0"/>

            <Button Grid.Column="1"
                    Content="↻"
                    Command="{Binding LoadJiraIssuesCommand}"
                    ToolTip="Refresh JIRA issues" HorizontalAlignment="Center" Height="36" />
        </Grid>

        <TextBlock Grid.Row="1" 
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Manual Task Description (visible when NOT logged in) -->
        <TextBlock Grid.Row="2" 
                   Text="Task Description" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"
                   Visibility="{Binding IsJiraEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <TextBox x:Name="TaskDescriptionTextBox" 
                 Grid.Row="3" 
                 Text="{Binding TaskDescription, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15" 
                 MaxLength="100"
                 Visibility="{Binding IsJiraEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <!-- Duration -->
        <TextBlock Grid.Row="4" 
                   Text="Duration (minutes)" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>

        <TextBox x:Name="DurationTextBox" 
                 Grid.Row="5" 
                 Text="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15" 
                 MaxLength="4"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Start" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0" 
                    IsDefault="True"
                    Click="StartButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30" 
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
