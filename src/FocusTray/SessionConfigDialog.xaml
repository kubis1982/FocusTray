<Window x:Class="FocusTray.SessionConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FocusTray.Converters"
        Title="Start Focus Session" 
        Height="320" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="Height">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <converters:TimeOnlyToDateTimeConverter x:Key="TimeOnlyToDateTimeConverter"/>
        <converters:TimeOnlyStringConverter x:Key="TimeOnlyStringConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- JIRA Integration Section (visible when logged in) -->
        <Grid Grid.Row="0" 
              Margin="0,0,0,15"
              Visibility="{Binding IsJiraEnabled, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="JiraIssuesComboBox"
                      Grid.Column="0"
                      ItemsSource="{Binding JiraIssues}"
                      SelectedItem="{Binding SelectedIssue}"
                      DisplayMemberPath="DisplayText"
                      IsEnabled="{Binding IsLoadingIssues, Converter={StaticResource InverseBoolConverter}}"
                      Margin="0,0,10,0"/>

            <Button Grid.Column="1"
                    Content="↻"
                    Command="{Binding LoadJiraIssuesCommand}"
                    ToolTip="Refresh JIRA issues" HorizontalAlignment="Center" Height="36" />
        </Grid>

        <TextBlock Grid.Row="1" 
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <!-- Manual Task Description (visible when NOT logged in) -->
        <TextBlock Grid.Row="2" 
                   Text="Task Description" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"
                   Visibility="{Binding IsJiraEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <TextBox x:Name="TaskDescriptionTextBox" 
                 Grid.Row="3" 
                 Text="{Binding TaskDescription, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15" 
                 MaxLength="100"
                 Visibility="{Binding IsJiraEnabled, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <!-- Duration -->
        <TextBlock Grid.Row="4" 
                   Text="Duration" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>

        <!-- Duration mode selection (horizontal) -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,10">
            <RadioButton Content="Minutes" 
                         IsChecked="{Binding IsEndTimeMode, Converter={StaticResource InverseBoolConverter}}"
                         Margin="0,0,15,0"/>
            <RadioButton Content="Until time" 
                         IsChecked="{Binding IsEndTimeMode}"/>
        </StackPanel>

        <!-- Duration input (visible when NOT in end-time mode) -->
        <TextBox x:Name="DurationTextBox" 
                 Grid.Row="6" 
                 Text="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,15" 
                 MaxLength="4"
                 Visibility="{Binding IsEndTimeMode, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <!-- End time input (visible when in end-time mode) -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal"
                    Visibility="{Binding IsEndTimeMode, Converter={StaticResource BoolToVisibilityConverter}}"
                    Margin="0,0,0,15">
            <TextBlock Text="Target time:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding AvailableTimeSlots}"
                      SelectedItem="{Binding SelectedTimeSlot}"
                      Width="100">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat='HH:mm'}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Start" 
                    Width="80" 
                    Height="30" 
                    Margin="0,0,10,0" 
                    IsDefault="True"
                    Click="StartButton_Click"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Height="30" 
                    IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
