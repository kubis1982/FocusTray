<Window x:Class="FocusTray.Views.TeamsLoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login to Microsoft Teams" 
        Height="Auto" 
        SizeToContent="Height"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Login to Microsoft Teams" 
                   FontSize="20" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Description -->
        <TextBlock Grid.Row="1" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,20">
            <Run Text="Click the button below to sign in with your Microsoft account."/>
            <LineBreak/>
            <Run Text="A browser window will open for authentication."/>
        </TextBlock>

        <!-- Status Message -->
        <TextBlock Grid.Row="2" 
                   Name="StatusTextBlock"
                   TextWrapping="Wrap" 
                   Margin="0,0,0,15"
                   MinHeight="20"/>

        <!-- Progress Indicator -->
        <ProgressBar Grid.Row="3" 
                     Name="ProgressBar"
                     Height="5" 
                     IsIndeterminate="True"
                     Margin="0,0,0,15"
                     Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Name="LoginButton" 
                    Content="Sign in with Microsoft" 
                    Width="180" 
                    Height="35" 
                    Click="Login_Click"
                    Margin="0,0,10,0"/>
            <Button Name="CancelButton" 
                    Content="Cancel" 
                    Width="100" 
                    Height="35" 
                    Click="Cancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
